# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle

name: Backend CI

concurrency:
  group: backend-ci-${{ github.ref }}
  cancel-in-progress: true

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
      # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run lint check
        run: ./gradlew checkstyleMain checkstyleTest
        continue-on-error: true # 체크스타일 실패해도 리포트를 보여주기 위해 계속 진행

      - name: Upload checkstyle report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: checkstyle-report
          path: build/reports/checkstyle/

      - name: test
        run: ./gradlew test --no-daemon --stacktrace

      ## ===========
      ## 테스트 결과를 GitHub Checks UI에 업로드
      ## - if: always() → 성공, 실패 여부와 관계없이 실행
      ## - files → JUnit XML 결과 파일 경로
      ## - check_name → Checks 탭에 표시될 이름
      ## - check_run_annotations: none → 코드 줄 옆 ❌ 표시 비활성화
      ## - comment_mode: off → PR 코멘트 남기지 않고 Checks UI에만 표시
      ## ===========
      - name: Publish Unit Test Results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: build/test-results/test/TEST-*.xml
          check_name: '테스트 결과 🛠️'
          check_run_annotations: 'none'
          comment_mode: 'off'

        # 3. 빌드된 Jar 파일 보관 (성공했을 때만)
        #      - name: Upload Build Artifact
        #        uses: actions/upload-artifact@v4
        #        if: success()
        #        with:
        #          name: backend-build-jar
        #          path: build/libs/*.jar
        #          retention-days: 5 # 5일 뒤 자동 삭제
        #

        # NOTE: The Gradle Wrapper is the default and recommended way to run Gradle (https://docs.gradle.org/current/userguide/gradle_wrapper.html).
        # If your project does not have the Gradle Wrapper configured, you can use the following configuration to run Gradle with a specified version.
        #
        # - name: Setup Gradle
        #   uses: gradle/actions/setup-gradle@af1da67850ed9a4cedd57bfd976089dd991e2582 # v4.0.0
        #   with:
        #     gradle-version: '8.9'
        #
        # - name: Build with Gradle 8.9
      #   run: gradle build
